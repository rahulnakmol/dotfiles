# 1Password SSH agent — active only when the agent socket exists
# Works on native Linux (1Password app) and WSL (Windows relay)
Match host * exec "test -S %d/.1password/agent.sock"
    IdentityAgent ~/.1password/agent.sock
