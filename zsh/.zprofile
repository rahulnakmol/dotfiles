# Shell Hooks
#eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"                                                     # Initialize Homebrew
eval "$(starship init zsh)"                                                                                 # Initialize Starship prompt
eval "$(zoxide init --cmd cd zsh)"                                                                          # Initialize zoxide
#eval "$(brew --prefix rustup)/bin"
